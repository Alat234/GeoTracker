
services:
  maria-db:
    image: mariadb
    environment:
      MARIADB_DATABASE: tracker
      MARIADB_USER: root
      MARIADB_PASSWORD: root
      MARIADB_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db-store:/var/lib/mysql

volumes:
  db-store:
